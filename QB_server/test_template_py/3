







if filter_long_strings(["hello", "world", "this", "is", "a", "test"]) == ["hello", "world"]:
    if filter_long_strings(["apple", "banana", "cherry", "date"]) == ["banana", "cherry"]:
        if filter_long_strings(["Python", "is", "a", "popular", "programming", "language"]) == ["Python", "popular", "programming", "language"]:
            if filter_long_strings(["ab", "cd", "ef", "gh", "ij"]) == []:
                if filter_long_strings(["abcdefg", "hijklmnop", "qrstuvwxyz"]) == ["abcdefg", "hijklmnop", "qrstuvwxyz"]:
                    print("0")
                else:
                    print("Failed test case 5")
            else:
                print("Failed test case 4")
        else:
            print("Failed test case 3")
    else:
        print("Failed test case 2")
else:
    print("Failed test case 1")